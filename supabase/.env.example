# === Servidor / Edge Functions — NUNCA no frontend, NUNCA versionar ===
# Configure via `supabase secrets set NOME=valor` (ou no .env local do backend).

SUPABASE_URL=https://ncmfrykmslnpvslmxgof.supabase.co
SUPABASE_SERVICE_ROLE_KEY=<COLOQUE_A_SERVICE_ROLE_AQUI_VIA_ENV>   # ignora RLS — admin
SUPABASE_SECRET_KEY=<COLOQUE_A_SECRET_KEY_sb_secret_AQUI_VIA_ENV> # admin
DATABASE_URL=<CONNECTION_STRING_POSTGRES_DO_SUPABASE>             # p/ migrations/Prisma
GEMINI_API_KEY=<CHAVE_DO_GEMINI_VIA_ENV>                          # Edge Function gemini
ANTHROPIC_API_KEY=<CHAVE_DO_CLAUDE_VIA_ENV>                        # Edge Function treino
ANTHROPIC_MODEL=claude-sonnet-5                                    # opcional
VAPID_PUBLIC_KEY=<VAPID_PUBLIC>                                    # Web Push (pública tb no front)
VAPID_PRIVATE_KEY=<VAPID_PRIVATE>                                  # Web Push — JAMAIS no front
VAPID_SUBJECT=mailto:voce@exemplo.com                             # contato VAPID

# Segurança:
# - Se qualquer chave secreta vazar, ROTACIONE em Settings → API do painel Supabase.
# - As chaves reais que estavam no appsettings.json original devem ser rotacionadas.
