# === Frontend (público — pode ir no bundle, protegido por RLS) ===
VITE_SUPABASE_URL=https://ncmfrykmslnpvslmxgof.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_troque_por_sua_anon_publishable_key
# Chave pública VAPID (Web Push). A PRIVADA nunca vem para o frontend.
VITE_VAPID_PUBLIC_KEY=cole_a_vapid_public_key_aqui

# NUNCA coloque SERVICE_ROLE, SECRET_KEY, DATABASE_URL ou GEMINI_API_KEY aqui.
# Segredos ficam apenas no servidor / Edge Functions (ver ../supabase/.env.example).
